- args:
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    sql: CREATE TABLE "public"."post"("id" uuid NOT NULL DEFAULT uuid_generate_v4(),
      "title" text NOT NULL, "content" text NOT NULL, "createdAt" timestamptz NOT
      NULL DEFAULT now(), PRIMARY KEY ("id") );
  type: run_sql
- args:
    name: post
    schema: public
  type: add_existing_table_or_view

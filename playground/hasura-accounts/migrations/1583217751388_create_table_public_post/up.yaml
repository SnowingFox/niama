- args:
    cascade: false
    read_only: false
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "public"."post"("id" uuid NOT NULL DEFAULT uuid_generate_v4(),
      "author" uuid NOT NULL, "category" text NOT NULL, "content" text NOT NULL, "title"
      text NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("author") REFERENCES "public"."user"("id")
      ON UPDATE restrict ON DELETE restrict, FOREIGN KEY ("category") REFERENCES "public"."post_category"("value")
      ON UPDATE restrict ON DELETE restrict, UNIQUE ("id"));
  type: run_sql
- args:
    name: post
    schema: public
  type: add_existing_table_or_view
